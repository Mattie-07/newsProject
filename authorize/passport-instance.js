// const passport = require('passport');

// Object.freeze(passport);
// module.exports = passport;

// would be used to make sure there is only one instance of passport. Object.freeze is a software design pattern called the Singleton pattern